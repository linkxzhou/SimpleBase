<script setup lang="ts">
import type { HTMLAttributes } from 'vue'
import { cn } from '@/lib/utils'

const props = withDefaults(defineProps<{
  class?: HTMLAttributes['class']
  size?: 'default' | 'sm'
}>(), {
  size: 'default',
})
</script>

<template>
  <div
    data-slot="card"
    :data-size="size"
    :class="cn('bg-card text-card-foreground rounded-xl border border-border shadow-xs overflow-hidden flex flex-col data-[size=sm]:text-xs group/card', props.class)"
  >
    <slot />
  </div>
</template>
