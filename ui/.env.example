# 是否使用内置 mock 数据（true/false）
VITE_USE_MOCK=true
# 后端 API 基础路径：留空时请求直接发往相对路径（/v1/...），
# 开发环境由 vite proxy 转发到 localhost:8080，生产环境由后端直接处理。
VITE_API_BASE_URL=
# WebSocket 基础地址（可选，默认同源推导）
# VITE_WS_BASE_URL=ws://localhost:8080
