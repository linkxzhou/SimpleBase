# browser-quickstart

1. `cd packages/js-sdk && yarn && yarn build`
2. From repo root, serve with any static server that allows parent paths, e.g.:

```bash
npx serve -l 5174 .
# open http://127.0.0.1:5174/examples/browser-quickstart/
```

Do **not** put a write-capable API key in a public site.
